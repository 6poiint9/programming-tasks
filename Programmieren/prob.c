#include <stdio.h>

int func(int input, int coins[], int size);

int main() 
{
  int amount;
  int size;
  int coins[size];
  // scanf the amount and the coins 
  scanf("%d", &amount);
  scanf("%d", &size);
  for (int i = 0; i < size; i++) {
       scanf("%d", &coins[i]);
    }
   
  int result = func(amount, coins, size);

  return 0;
}


int func(int input, int coins[], int size) {
 
 if () 

}
